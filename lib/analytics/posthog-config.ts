// PostHog configuration disabled temporarily
// To re-enable, add PostHog via their script tag method instead of SDK
export const posthogConfig = {
  token: "",
  host: "https://us.i.posthog.com",
}
